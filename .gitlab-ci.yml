stages:
  - sync-git
  - build
  - check-lint
  - test

cache:
  paths:
    - node_modules/

sync:
  stage: sync-git
  script: 
    - git remote add github https://github.com/BryakunovIvan/password-manager-client.git
    - git push --all github
